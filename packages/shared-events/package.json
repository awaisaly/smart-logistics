{
  "name": "@smartlogistics/shared-events",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "dev": "tsc -w -p tsconfig.json",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "echo 'lint shared-events'",
    "test": "echo 'no tests for shared-events'",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "kafkajs": "^2.2.4",
    "zod": "^3.24.2"
  }
}
